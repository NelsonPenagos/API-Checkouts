server:
  port: ${PORT:8888}

spring:
  #Spring Cloud Config Server Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NelsonPenagos/API-Checkouts
          searchPaths: config-properties